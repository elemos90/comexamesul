# .htaccess NA RAIZ - Redirecionar para /public/

# Desabilitar listagem de diretórios
Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /comexamesul/
    
    # Permitir arquivos estáticos da pasta public
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    
    # Redirecionar tudo para index.php na raiz
    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>

# Bloquear acesso a arquivos sensíveis
<FilesMatch "\.(env|md|sql|log|ini|sh|bak|config|json|lock)$">
    Require all denied
</FilesMatch>

# Bloquear acesso direto a pastas sensíveis (usando RewriteRule dentro do módulo)
